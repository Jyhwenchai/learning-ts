"use strict";
{
    // 类型断言
    // 有时候 TypeScript 无法推断出一个值的类型，那么可以使用 as 进行类型断言，明确指出其类型。
    const myCanvas = document.getElementById("main_canvas");
    // 这里的写法与前面的写法等价(除了在 .tsx 文件中)
    const myCanvas2 = document.getElementById("main_canvas");
}
